apiVersion: v1
kind: Service
metadata:
  name: simple-web-app-service
  namespace: simple-web-app
spec:
  type: NodePort
  selector:
    app: simple-web-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080